" set compatibility to Vim only
set nocompatible

" Encoding
set encoding=utf-8

" Wrapping the text that is beyonf the screen
set wrap

filetype on

filetype plugin on

filetype indent on

syntax on

" Show line numbers
set number
